# Core API
fastapi>=0.109.2,<1.0.0
uvicorn[standard]>=0.31.1,<1.0.0
gunicorn~=25.3.0
python-multipart~=0.0.18
pydantic>=2.12.4,<2.14.0
pydantic-settings~=2.12.0

# OAuth (Sprint 157)
authlib>=1.3.0
itsdangerous>=2.2.0

# Magic Link Email Auth (Sprint 224)
resend>=2.0.0

# Error Tracking (Production Hardening)
sentry-sdk[fastapi]>=2.58.0,<3.0.0

# Database
sqlalchemy>=2.0.27,<3.0.0
asyncpg>=0.30.0,<1.0.0
# psycopg2-binary removed Sprint 154 — psycopg[binary]>=3.1.0 (line 29) is the modern async replacement
alembic~=1.15.0
pgvector==0.2.5

# --- AI CORE STACK (PINNED to tested minor versions) ---
# Phase 0: langchain + langchain-community removed (0 imports in app/)
# Phase 1-2: langchain-openai, langchain-google-genai, langchain-ollama, langchain-text-splitters removed
# All providers now use WiiiChatModel (AsyncOpenAI SDK wrapper)
langchain-core>=1.2.18,<1.4.0

# LangGraph REMOVED (De-LangGraphing Phase 3) — replaced by WiiiRunner (app/engine/multi_agent/runner.py)
# langgraph, langgraph-checkpoint, langgraph-checkpoint-postgres, langgraph-prebuilt — all removed
# psycopg3 async driver (kept for asyncpg compat)
psycopg[binary]>=3.1.0,<4.0.0

# Unified LLM Client (Sprint 55: AsyncOpenAI SDK for OpenAI-compatible endpoints)
openai~=2.32.0

# MCP Support (Sprint 56: Model Context Protocol)
mcp~=1.26.0
fastapi-mcp~=0.4.0
langchain-mcp-adapters>=0.2.2,<1.0.0
opensandbox>=0.1.5,<0.2.0
opensandbox-code-interpreter>=0.1.1,<0.2.0

# Google GenAI SDK (UNIFIED - CHỈ THỊ 26 + Semantic Memory)
# SDK mới thống nhất cho Gemini API - hỗ trợ cả embeddings và vision
# NOTE: Không dùng google-generativeai vì conflict với langchain-google-genai
google-genai>=1.66.0,<2.0.0

# Semantic Memory
numpy>=1.24.0
scikit-learn>=1.3.0

# Graph DB
neo4j~=5.28.0

# PDF Processing
pypdf>=4.0.0
pymupdf>=1.26.0  # CHỈ THỊ 26: PDF to image (no external deps, replaces pdf2image+Poppler)
pdf2image>=1.16.3  # Fallback for Poppler-based PDF rendering (pymupdf is primary)
Pillow>=10.0.0  # CHỈ THỊ 26: Image processing

# Document Conversion (Course Generation — design spec v2.0, optional)
# Install: pip install docling  (requires 3-4GB for DocLayNet + TableFormer models)
# Or use Docling Serve: docker pull doclingproject/docling-serve
# Feature-gated: USE_DOCLING_FOR_COURSE_GEN=true
# docling>=2.30.0  # Uncomment when ready to deploy Docling

# Object Storage (MinIO SDK)
minio>=7.2.0

# Web Search
# ddgs removed Sprint 154 — duckduckgo-search>=7.0.0 is the active package (import: duckduckgo_search)
duckduckgo-search>=7.0.0
feedparser>=6.0.0  # Sprint 102: RSS news feeds (Vietnamese news aggregation)

# Utils
httpx>=0.28.1
PyJWT[crypto]>=2.12.1,<3.0.0
python-dotenv==1.0.1
slowapi~=0.1.9
pyyaml>=6.0.0  # CHỈ THỊ SỐ 16: YAML config for personas

# Observability (SOTA 2026: OpenTelemetry GenAI Semantic Conventions)
opentelemetry-api>=1.41.1
opentelemetry-sdk>=1.41.1
opentelemetry-exporter-otlp>=1.41.1

# LangSmith (Sprint 144b: LLM tracing dashboard)
langsmith>=0.4.0,<1.0.0

# Structured Logging (SOTA 2026)
structlog>=25.5.0

# Excel Report Generation (Sprint 148: Product Search)
xlsxwriter>=3.2.0
openpyxl>=3.1.0
python-docx>=1.1.0

# HTML Parsing (Sprint 150: Product Page Scraper)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Browser Scraping (Sprint 152: Playwright headless Chromium + LLM extraction)
# Optional: only needed when enable_browser_scraping=True
# Install browser: playwright install chromium
playwright>=1.49.0

# Telegram Bot (Sprint 12: Multi-Channel Gateway, optional)
aiogram>=3.15.0,<4.0.0

# Background Tasks (Sprint 18: Valkey + Taskiq, optional)
taskiq>=0.11.0,<1.0.0
taskiq-redis>=1.0.0,<2.0.0

# Testing
pytest>=7.4.4
pytest-asyncio>=0.23.4
pytest-cov>=4.1.0
hypothesis>=6.92.0
